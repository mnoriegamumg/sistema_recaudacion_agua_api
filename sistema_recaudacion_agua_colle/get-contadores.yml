info:
  name: get-contadores
  type: http

http:
  method: GET
  url: http://localhost:8000/api/contadores
  headers:
    - name: Authorization
      value: Bearer {TOKEN}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
